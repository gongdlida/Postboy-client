import React from "react";
const fetch = require("node-fetch");

let SendReq = (props) => {
  console.log("asdfadsfsa", props.reqForm);
  return <></>;
  //   let one = await fetch({});
};

export default SendReq;
